import { RECOMMENDATION_TYPES } from './recommendationTypes';

// Configuração padrão para formulário de recomendação
export const DEFAULT_FORM_DATA = {
  selectedPreferences: [],
  selectedFeatures: [],
  selectedRecommendationType: RECOMMENDATION_TYPES.MULTIPLE_PRODUCTS,
};
